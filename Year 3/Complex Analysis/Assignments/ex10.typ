#import "../../../src/article.typ": *
#show: article.with(
  title: [ פתרון מטלה 10 --- פונקציות מרוכבות, 80519 ],
  signature: [#align(center)[#image("../../../src/duck.png", width: 30%, fit: "contain")]],
)

#question()
בכל סעיף נמצא את הנקודות הסינגולריות של הפונקציה, נסווגן ונחשב בהן את השארית.

#subquestion()
$f(z)=z^(2n)/(z+1)^n$ לכל $n in NN$.

#solution[
  המכנה נעלם כאשר
  $ z+1 = 0 <==> z=-1 $
  ונטען שהיא נקודה סינגולרית מסוג קוטב: נזכיר ש־$z_0=-1$ תהיה קוטב מסדר $m$ אם קיימת פונקציה הולומורפית $g(z)$ עבורה $g(z_0)!=0$ המקיימת
  $ f(z) = (g(z))/(z-z_0)^m $
  בסביבה מנוקבת של $z_0$.\
  נגדיר $g(z) = z^(2n)$ זה המונה והוא פולינום ולכן פונקציה שלמה ומתקיים
  $ g(-1) = (-1)^(2n) = 1 != 0 $
  אז המונה היא פונקציה הולומורפית בסביבת $z_0=-1$ והיא לא נעלמת בנקודה.\
  כלומר $z_0=-1$ היא נקודה סינגולרית מסוג קוטב מסדר $n$.\
  נזכר בטענה מהתרגול: אם $f in Hol(G without {a})$ ו־$a$ קוטב מסדר $n$ אז
  $ res_f (a) = ((dif/(dif z))^(n-1) ((z-a)^n f(z))|_(z=a))/(n-1)! = (((z-a)^n f(z))^(n-1)(a))/(n-1)! $
  ובהתאם במקרה שלנו
  $
    res_f (-1) = ((dif/(dif z))^(n-1) ((z+1)^n z^(2n)/(z+1)^n)|_(z=-1))/(n-1)! = ((dif/(dif z))^(n-1) (z^(2n)|_(z=-1)))/(n-1)! = (2n)!/(2n-(n-1))! z^(2n-(n-1))|_(z=-1) 1/(n-1)! \
    = (2n)!/((n+1)!(n-1)!) (-1)^(n+1)
  $
]

#subquestion()
$f(z)=z^2cos(1/(z-2))$.

#solution[
  המכנה של $cos$ לא מוגדר כאשר $z_0=2$.\
  ניזכר בפיתוח טיילור של $cos(w)$:
  $ cos(w) = sum_(k=0)^infinity (-1)^k/(2k)! w^(2k) $
  בהצבה של $w = 1/(z-2)$ נקבל
  $ cos(1/(z-2)) = sum_(k=0)^infinity (-1)^k/(2k)! 1/(z-2)^(2k) $
  ובמכפלה של $z^2$
  $ z^2 cos(1/(z-2)) = sum_(k=0)^infinity (-1)^k/(2k)! z^2/(z-2)^(2k) $
  נשים לב שהחזקות הן שליליות ולכן יש לנו פה פיתוח לורן אינסופי ולפי הטענה שראינו בהרצאה ובתרגול נובע ש־$z_0=2$ היא נקודה סינגולרית עיקרית (יש אינסוף חזקות שליליות).\
  לפי הגדרה, $res_f (-1)$ יהיה המקדם של $c_(-1)$ בפיתוח לורן סביב $z_0=2$.\
  נשים לב
  $ z^2= ((z-2)+2)^2 = (z-2)^2+4(z-2)+4 $
  ולכן
  $
    z^2 cos(1/(z-2))= ((z-2)^2+4(z-2)+4) cos(1/(z-2)) = ((z-2)^2 + 4(z-2)+4) sum_(k=0)^infinity (-1)^k/(2k)! 1/(z-2)^(2k)
  $
  כלומר אנחנו מחפשים את המקדמים של $(z-2)^(-1)$ ולכן נפתח סוגריים לכל מקדם
  $
    (z-2)^2 dot.op (z-2)^(-2k) = (z-2)^(2-2k) ==> 2-2k = -1 <==> 3 = 2k <==> k = 3/2 space crossmark \
    4(z-2) dot.op (z-2)^(-2k) ==> -2k+1 = -1 <==> -2k = -2 <==> k = 1 space checkmark \
    4 dot.op (z-2)^(-2k) ==> -2k = -1 <==> k = 1/2 space crossmark
  $
  כלומר רק הביטוי השני רלוונטי ונקבל עבור $k=1$
  $ (-1)^1/(2)! (z-2)^(-2) = -1/2 (z-2)^(-2) $
  ועם המקדם
  $ 4(z-2) dot.op -1/2 (z-2)^(-2) = -2(z-2)^(-1) $
  ולכן
  $ res_f (2) = -2 $
]

#subquestion()
$f(z) = 1/sin(1/z)$.

#solution[
  ראינו ש־$sin(w)$ היא פונקציה שלמה ולכן יש נקודה סינגולרית היכן שהמכנה מתאפס, כלומר
  $ sin(w)=0 <==> w = k pi (space k in ZZ) ==> z = 1/(k pi) $
  כלומר
  $ E colon.eq {1/(k pi) bar k in ZZ without {0}} union {0} $
  כלומר $E$ זה אוסף הנקודות הסינגולריות של $f$.\
  נטען שלכל $k in ZZ$, הנקודה $z_k=1/(k pi)$ היא נקודה סינגולרית מבודדת מסוג קוטב (הגבול בשאיפה לנקודה הוא אינסוף), אז נגזור ונקבל
  $
    dif/(dif z) (sin(1/z))|_(z=pi k)=-cos(1/z)/z^2|_(z=1/(pi k)) = -cos(1/(pi k))/(pi k)^2 = (-1)^(k+1)/(pi^2 k^2) != 0
  $
  כלומר $z_k$ היא נקודה סינגולרית מסוג קוטב מסדר $1$.\
  נזכר בטענה שראינו בהרצאה: אם $f(z)=(phi(z))/(psi(z))$ מקיימת $psi(a)=0, phi(a) !=0, psi'(a) !=0$ אז $res_f (a) = (phi(a))/(psi(a))$.\
  ואכן כפי שראינו $psi(z) = sin(1/z)$ ו־$psi'(z) != 0$, נשתמש בטענה ונקבל
  $ res_f (z_k) = 1/((-1)^(k+1) pi^2 k^2) = (-1)^(k+1)/(pi^2 k^2) $
  כעת, עבור $z=0$ נשים לב שיש לנו נקודה סינגולרית לא מבודדת (כי עבור כל $R$ רדיוס סביב הראשית ניתן למצוא $k in NN$ כך ש־$1/(pi k)$ נמצא בכדור ברדיוס $R$ סביב הראשית) ולכן לא ניתן למצוא לה שארית.
]

#question()
נוכיח שלכל $n in NN$ מתקיים
$ integral_(-infinity)^infinity 1/(x^2+1)^(n+1) dif x = pi (2n-1)!!/(2n)!! $
כאשר עצרת כפולה מוגדרת על־ידי;;
$ n!! colon.eq product_(k=0)^(ceil(n/2)-1) (n-2k) = n(n-2)(n-4) dots.h.c $

#proof[
  נגדיר
  $ f(z) = 1/((z^2+1)^(n+1))=1/((z-i)^(n+1)(z+i)^(n+1)) $
  והאפסים של $f$ הם ב־$plus.minus i$ והם קטבים מסדר $n+1$ והם מבודדים (כי לפולינומים האפסים מבודדים) והם אכן קטבים כי אם נכתוב
  $ f(z) = g(z)/(z-i)^(n+1), space g(z) = 1/(z+i)^(n+1) $
  אז $g$ הולומורפית בסביבה של $z=i$ ו־$g(i)!=0$ אז מההגדרה זה קוטב ובאופן דומה למקרה השני.\
  אנחנו רוצים אינטגרל על $(-infinity, infinity)$ ולכן ניקח את $C_R$ להיות הישר/עקומה שמכיל את $[-R,R]$ ואת
  $ Gamma_R colon.eq {z = R e^(i theta) bar theta in [0,pi]} $
  בחצי המישור העליון ונקבל
  $ integral_(C_R) f(z) dif z = integral_(-R)^R f(x) dif x + integral_(Gamma_R) f(z) dif z $
  בצורה כזאת נכיל את הנקודה הסינגולרית $z=i$ וממשפט השארית של קושי נקבל
  $ integral_(C_R) f(z) dif z = 2pi i res_f (i) $
  נשים לב שעל $Gamma_R$ מתקיים $abs(z)=R$ ולכן
  $ abs(z^2+1)>= abs(z)^2-1 = R^2-1 ==> abs(f(z))<= 1/(R^2-1)^(n+1) $
  וכן
  $ abs(integral_(Gamma_R) f(z) dif z)<=(pi R )/(R^2-1)^(n+1) stretch(arrow.r)_(R arrow.r infinity) 0 $
  וכאשר $R arrow.r infinity$ מתקיים
  $
    integral_(-R)^R f(x) dif x stretch(arrow.r)_(R arrow.r infinity) integral_(-infinity)^infinity 1/(x^2+1)^(n+1) dif x
  $
  כלומר כתבנו בצורה שונה ("שיטת לא עשיתי כלום" כדי לעבור לתנאים של המשפטים שאנחנו מכירים) וקיבלנו
  $ integral_(-infinity)^infinity 1/(x^2+1)^(n+1) dif x = 2pi i res_f (i) $
  נרצה להשתמש בטענה אודות שארית של פונקציה בנקודה סינגולרית מסוג קוטב מסדר $n$:
  $ res_f (a) = (dif^(n-1)/(dif z^(n-1)) ((z-a)^n f(z))|_(z=a))/(n-1)! $
  ובמקרה שלנו בגלל שזה קוטב מסדר $n+1$ נקבל
  $
    res_f (i) = 1/n! dif^n/(dif z^n) ((z-i)^(n+1) 1/((z+i)^(n+1)(z-i)^(n+1)))|_(z=i)=1/n! (-1)^n (2n)!/n! (z+i)^(-2n+1)|_(z=i) = (-1)^n (2n)!/(n!)^2 (2i)^(-2n+1)
  $
  נפשט את הביטוי
  $ (2i)^(-2n+1) = 2^(2n+1)+(-1)^n i $
  ולכן
  $
    (-1)^n (2n)!/(n!)^2 (2i)^(-2n+1) = cancel((-1)^n) (2n)!/(n!)^2 1/(2^(2n+1) cancel((-1)^n) i) = (2n)!/(2^(2n+1) (n!)^2 i)
  $
  ולכן
  $
    integral_(-infinity)^infinity 1/(x^2+1)^(n+1) dif x = 2pi i res_f (i) = 2pi i (2n)!/( 2^(2n+1) (n!)^2 i) = pi (2n)!/(2^(2n) (n!)^2)
  $
  נשים לב
  $$
  ואם נקבץ למכפלות זוגיות ולמכפלות אי־זוגיות בנפרד נקבל
  $
    (star) space (2n)! = 1 dot.op 2 dot.op 3 dot.op 4 dot.op dots.h.c dot.op (2n-1) dot.op (2n) = (1 dot.op 3 dot.op 5 dot.op dots.h.c dot.op (2n-1)) dot.op (2 dot.op 4 dot.op 6 dot.op dots.h.c dot.op 2n) = (2n-1)!! (2n)!!
  $
  ונשים לב שמהגדרת העצרת השנייה
  $
    (star star) space (2n)!! = 2n dot.op (2n-2) dot.op (2n-4) dot.op dots.h.c dot.op 4 dot.op 2 = (2 dot.op n) dot.op (2 dot.op (n-1)) dot.op (2 dot.op (n-2)) dot.op dots.h.c dot.op (2 dot.op 2) dot.op (2 dot.op 1)=2^n n!
  $
  אז משילוב של $(star)$ ו־$(star star)$ נקבל
  $ (star star star) space (2n-1)!! = (2n)!/(2^n n!) $
  ואם נחזור לתוצאה שלנו נקבל
  $
    pi (2n)!/(2^(2n) (n!)^2)= pi (2n)!/((2^n n!)(2^n n!)) =_((star star)) pi (2n)!/((2n)!! (2^n n!)) =_((star star star)) pi (2n-1)!!/(2n)!!
  $
  שזה בידיוק מה שרצינו להוכיח.
]

#question()
יהיו $W = CC without [-1,1]$ ו־$f: CC without {-1,1} arrow.r CC$ הפונקציה $f(z)=2/(1-z^2)$.

#subquestion()
נמצא את נקודות הסינגולריות של $f$ ונחשב את השארית בהן.

#proof[
  נבחין שהמכנה של $f$ הוא פולינום ולכן שלם ונקודות הסינגולריות של $f$ תהיינה רק כאשר המכנה מתאפס כלומר $ 1-z^2=0 <==> z_0 = plus.minus 1 $
  אלו נקדוות סינגולריות מסוג קוטב מסדר $1$ ממבחן הנגזרת ואלו הם קטבים שכן כאשר $z arrow.r plus.minus 1$ המכנה שואף לאפס וכל הביטוי שואף לאינסוף.\
  נשתמש במבחן המנה לחישוב שארית (הטענה שציטטנו בשאלה 1 סעיף ג') ונקבל אם נסמן
  $ phi(z)=2, space psi(z) = 1-z^2 ==> psi'(z) = -2z ==> res_f (a) = (phi(a))/(psi'(a)) $
  ונקבל
  $
    res_f (1) = 2/(-2 dot.op 1) = 2/(-2) = -1 \
    res_f (-1) = 2/(-2 dot.op -1) = 2/2 = 1
  $
]

#subquestion()
נסיק שלכל מסילה $gamma$ שהיא סגורה ו־$C^1$ בתחום $W$ מתקיים $integral_gamma f(z) dif z =0$ ולכן יש ל־$f$ פונקציה קדומה $F$ ב־$W$.

#proof[
  ראינו כבר ש־$W$ הוא תחום קשיר וכל מסילה סגורה $gamma subset W$ לא חוצה את הקטע $[-1,1]$.\
  מבחינה גיאומטרית יש שתי אפשרויות – אם המסילה $gamma$ לא מקיפה את המקטע $[-1,1]$ אז $f$ הולומורפית בכל מקום בפנים של $gamma$ ולכן ממשפט קושי האינטגרל על $gamma$ הוא אפס.\
  אם לא, בהכרח שהמסילה היא מקיפה של שתי הנקודות, לא ייתכן שרק אחת מהן תהיה כי אז המסילה תהיה לא סגורה (הורדנו את הקטע $[-1,1]$) ואז במקרה זה ממשפט השארית של קושי מתקיים
  $ integral_( partial gamma) (f)z dif z = 2pi i(res_f (-1) + res_f (1))= 2pi i (-1+1) = 0 $
  עבור החלק השני, מכך שלכל מסילה הטענה לעיל מתקיימת נובע שהאינטגרל הקווי בין $z_0, z in W$ תלוי רק בנקודות הקצה ולא על המסילה עצמה, כלומר
  $ integral_(gamma_1) f(zeta) dif zeta = integral_(gamma_2) f(zeta) dif zeta $
  אז אם נגדיר
  $ F(z) integral_(z_0)^z f(zeta) dif zeta $
  היא מוגדרת היטב וכפי שראינו בתרגיל 8, $F$ הולומורפית ומתקיים $F'=f$.
]
#pagebreak()

#subquestion()
בתרגול 4 הראינו שניתן להגדיר את הפונקציה $g(z)=Log((z+1)/(z-1))$ בתחום $W$.\
נוכיח ש־$g$ היא קדומה של $f$ ב־$W$.

#proof[
  הגדרנו את הנגזרת הלוגריתמית על $f:G arrow.r CC$ שלא מתאפסת אף־פעם על־ידי $(dif/(dif z) f)/f$ ואכן על $W$ הפונקציה $h(z)=(z+1)/(z-1)$ לא מתאפסת (שכן $plus.minus 1 in.not W$), אז
  $ dif/(dif z) h(z) = (1 dot.op (z-1)-(z+1) dot.op (1))/(z-1)^2 = (-2)/(z-1)^2 $
  אז
  $ (h'(z))/(h(z)) = ((-2)/(z-1)^2)/((z+1)/(z-1))=(-2)/((z-1)(z+1))=(-2)/(z^2-1)=2/(1-z^2) = f(z) $
  היות והנגזרת של $g$ היא בידיוק $f$ בכל $W$ ולכן
  $ g'(z) = f(z) $
]

#subquestion()
נחשב את האינטגרל $integral_(abs(z)=2) g(z) dif z$.

#solution[
  נשים לב ש־$plus.minus 1$ נמצאים על המעגל $abs(z)=2$ אבל $g$ הולומורפית על $abs(z)>1$ אז נוכל להסתכל על השארית באינסוף
  $ integral_(abs(z)=2) g(z) dif z = -2pi res_g (infinity) = 2pi i c_(-1) $
  שכן $res_g (infinity)=-c_(-1)$.\
  אז עבור $abs(z)>1$ נקבל
  $ g(z)=Log((z+1)/(z-1))=Log((z(1+1/z))/(z(1-1/z)))=Log(1+1/z)-Log(1-1/z) $
  סדרת המהלכים הזאת לא טריוויאלית ולא נכונה תמיד (זה נכון רק במודלו $2pi i$), אבל אפשר לראות שהם שקולים לפי הנגזרת: נסמן $w=1/z$
  $ dif/(dif w) (Log(1+w)-Log(1-w))=1/(1+w)-(-1)/(1-w) = ((1-w)+(1+w))/(1-w^2)=2/(1-w) $
  הנגזרות זהות ומסכימות ב־$0$ אז עבור $abs(w)<1$ כלומר $abs(z)>1$ הן זהות (עשינו משהו דומה בהרצאה עם עדי כמובן בהערה 2.7.2 בסיכום שלה).\
  נמשיך ולפי פיתוח טיילור
  $
    Log(1+1/z)=1/z-1/(2z^2)+1/(3z^3) - dots.h.c \
    Log(1-1/z)=1/(-z) -1/(2z^2)-1/(3z^3) - dots.h.c
  $
  אז
  $ g(z)=(1/z-1/(2z^2)+1/(3z^3) - dots.h.c)-(1/(-z) -1/(2z^2)-1/(3z^3) - dots.h.c)= 2/z + 2/(3z^3) dots.h.c $
  חישבנו חלק כי החזקות רק עולות ולכן זה מספיק.\
  ואם כך
  $ integral_(abs(z)=2) g(z) dif z = 2pi i c_(-1) = 4 pi i $

]


#question()
נגדיר $f(z) = pi/(z^2 tan(pi z))$

#subquestion()
לכל $n in NN$ נסמן ב־$C_n$ את המסילה המקיפה את הריבוע $[-n-1/2, n+1/2]^2$ נגד כיוון השעון. נוכיח
$ lim_(n arrow.r infinity) integral_(C_n) f(z) dif z = 0 $

#proof[
  נגד כיוון השעון $<==$ אוריאנטצייה חיובית.\
  לריבוע הנתון יש אורך צלע של $2n+1$ ולכן
  $ L(C_n) = 4(2n+1) = 8n+4 = O(n) $
  עבור $z in C_n$ מתקיים
  $ abs(z)>=n+1/2 ==> 1/abs(z)^2 <= 1/(n+1/2)^2 $
  האפסים של $tan(pi z)$ מתרחשים כאשר $z=k$ עבור $k in ZZ$ והקטבים הם כאשר $z=k+1/2$ עבור $k in ZZ$, כלומר לכל $n$, $C_n$ לא חותך את $tan(pi z)$ באזורי הקטבים/אפס ואפילו בפרט הם במרחק של $delta=1/2$ מהשפה $(star)$.\
  היות ו־$tan(pi z)$ היא פונקציה הולומורפית בסביבה פתוחה של $C_n$ והיא איננה מתאפסת על $C_n$ ו־$C_n$ קומפקטית (סגורה וחסומה ולכן היינה־בורל) אז היא בהכרח מקבלת מינימום חיובי (בערך מוחלט), כלומר
  $ exists m_n > 0 space s.t. space forall z in C_n space abs(tan(pi z))>=m_n $
  אבל $tan(pi z)$ היא מחזורית עם מחזור אחד כלומר
  $ tan(pi(z+k))=tan(pi z) $
  נגדיר
  $ S colon.eq {z in CC bar -1/2 <= re(z) <= 1/2} $
  ונשים לב שמתקיים
  $ C_n subset union.big_(k in ZZ) (S+k) $
  כי מהגדרת $C_n$ רק ביצענו הזזה ולכן כל $abs(tan(pi z)) in C_n inter S$, אז אם נגדיר
  $ K colon.eq {z in S bar dist(z, ZZ union (ZZ + 1/2))>=1/2} $
  זו קבוצה קומפקטית (סגורה וחסומה), $tan(pi z)$ הולומורפית ורציפה ולא מתאפסת בסביבה שלה ומ־$(star)$ כל נקודה ב־$C_n$ מתאימה עד־כדי $mod ZZ$ לנקודה ב־$K$ אז ממשפט ויירשטראס למינימום ומקסימום נובע
  $ m colon.eq min_(z in K) abs(tan(pi z))>0 $
  נשאר להראות שהחסם הזה נכון על כל $C_n$: יהי $z in C_n$ ולכן קיים $k in ZZ$ כך ש־$z-k in K$ ומהמחזוריות
  $ abs(tan(pi z))=abs(tan(pi(z-k)))>=m $
  אז נובע מכך
  $ abs(f(z))=abs(pi/(z^2 tan(pi z)))<=pi/(m(n+1/2)^2)= O(1/n^2) $
  אז בסך־הכל
  $
    abs(integral_(C_n) f(z) dif z)<= max_(z in C_n) abs(f(z)) dot.op L(C_n) = pi/(m(n+1/2)^2) dot.op (8n+4) = O(1/n^2) dot.op O(n) stretch(arrow.r)_(n arrow.r infinity) 0
  $
  כלומר
  $ lim_(n arrow.r infinity) integral_(C_n) pi/(z^2 tan(pi z)) dif z = 0 $
]

#subquestion()
נשתמש במשפט השארית כדי להסיק שמתקיים
$ sum_(n=1)^infinity 1/n^2 = pi^2/6 $

#proof[
  כל הנקודות הסינגולריות שלנו בתוך $C_n$ הם מהצורה
  $ z = {0, plus.minus 1, plus.minus 2, dots.h.c, plus.minus n} $
  ואלו הן נקודות מבודדות, אז ממשפט השארית של קושי
  $ integral_(C_n) f(z) dif z = 2pi i sum_(abs(k)<=n) res_f (k) $
  מהסעיף הקודם אנחנו יודעים
  $ lim_(n arrow.r infinity) integral_(C_n) f(z) dif z = 0 $
  ולכן
  $ sum_(k in ZZ) res_f (k) = 0 $
  ראשית נכתוב
  $ f(z)= pi/(z^2 tan(pi z)) = (pi cos(pi z))/(z^2 sin(pi z)) $
  אז עבור $z=k in ZZ without {0}$ נוכל להשתמש בטענה שציטטנו בשאלה 1 סעיף ג' ולקבל
  $ phi(z) = pi cos(pi z) space phi(z)=z^2sin(pi z) ==> psi'(z) = 2z sin(pi z)+pi z^2 cos(pi z) ==> psi'(k)=pi k^2 (-1)^k != 0 $ ולכן
  $ res_f (k) = (pi (-1)^k)/(pi k^2(-1)^k) = 1/k^2 $
  וזה אכן קוטב מסדר $1$ כי הנגזרת לא מתאפסת.\
  כמובן בגלל הסימטרייה נקבל שגם
  $ res_f (-k) = 1/k^2 $
  בשביל $z=0$ זה קוטב מסדר $3$ (כי $z^2$ קוטב מסדר $2$ ו־$sin(pi z)$ קוטב מסדר $1$ ולכן זה קוטב מסדר $3$ אם נשתמש בפיתוח טיילור של $sin(z)$) ולכן נשתמש בטענה שציטטנו בשאלה 1 סעיף א' ונקבל
  $
    res_f (0) = 1/2! dif^2/(dif z^2) (z^3 (pi cos(pi z))/(z^2 sin(pi z)))|_(z=0)= 1/2 dif^2/(dif z^2)(pi z cot(pi z))|_(z=0)
  $
  אני לא אוהבת נגזרות כאלו, אבל ניזכר שהשארית זה המקדם של $c_(-1)$ בפיתוח לורן:
  $ tan(z) = x + z^3/3 + (2z^5)/15 + dots.h.c ==> tan(pi z) = pi z + (pi z)^3/3 + (2(pi z)^5)/15 + dots.h.c $
  אז
  $ 1/tan(pi z) = 1/(pi z) dot.op 1/(1 + (pi z)^2/3 + dots.h.c) $
  אז עבור $u = (pi z)^2/3 + dots.h.c$ נקבל עם הנוסחה
  $ 1/(1+u) = 1-u+u^2 - dots.h.c ==> 1/tan(pi z) = 1/(pi z) (1-(pi z)^2/3 + dots.h.c) = 1/(pi z) - (pi z)/3 + dots.h.c $
  ועבור $f$ שלנו
  $ f(z) = pi/z^2 dot.op 1/tan(pi z) = pi/z^2 (1/(pi z) - (pi z)/3 + dots.h.c) = 1/z^3 - pi^2/(3z) + dots.h.c $
  ונקבל אם כך
  $ res_f (0) = -pi^2/3 $
  אז אם כך
  $
    0 = sum_(k in ZZ) res_f (k) <==> 0 = res_f (0) + sum_(k in NN) res_f (k) + sum_(k in NN) res_f (-k) <==> 0 = 2 sum_(k in NN) 1/k^2 -pi^2/3 <==> pi^2/3 = 2 sum_(k in NN) 1/k^2 <==> pi^2/6 = sum_(k=1)^infinity
  $
  ההוכחה באנליזה פונקציונלית יפה יותר.
]

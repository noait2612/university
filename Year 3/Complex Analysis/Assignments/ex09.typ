#import "../../../src/article.typ": *
#show: article.with(
  title: [ פתרון מטלה 09 --- פונקציות מרוכבות, 80519 ],
  signature: [#align(center)[#image("../../../src/duck.png", width: 30%, fit: "contain")]],
)

#question()
בכל אחד מהסעיפים נקבע האם קיימת פונקציה שלמה (הולומורפית ב־$CC$) המקיימת את התנאי הנתון. אם כן, נמצא אותה ואם לא נפריך.\
נשתמש במשפט היחידות השני: אם $G$ תחום ו־$f in Hol(G)$ ונניח שקיים $z_0 in G$ כך שיש סדרה $(z_n)_(n=1)^infinity subset G$ כך ש־$z_n arrow.r z_0$.\
אם לכל $n$, $f(z_n)=0$ אז $f eq.triple 0$.

#subquestion()
$f(1/n)=(-1)^n/n$ לכל $n in NN$.

#proof[
  נטען שלא קיימת פונקציה כזאת כי עבור כל $n in NN$ זוגי מתקיים
  $ f(1/(2n)) = 1/(2n) $
  כלומר זאת העתקת הזהות, אז נגדיר
  $ g(z) colon.eq f(z) - z $
  ונסתכל על הסדרה $z_n = 1/(2n)$, נקבל
  $ g(1/(2n))=f(1/(2n))-1/(2n) = 0 stretch(arrow.r)_(n arrow.r infinity) 0 $
  נפעיל את משפט היחידות השני על $g$ ונקבל $g eq.triple 0$, כלומר $f(z) = z$, אבל עבור $n=1$ נקבל
  $ f(1/1) = (-1)^1/1 = -1 != 1 $
  וזאת סתירה.
]

#subquestion()
$f(n)=(-1)^n n$ לכל $n in NN$.

#proof[
  כידוע, $-1 = e^(i pi)$ ולכן עם עיקרון דה־מואבר מההרצאה ראשונה
  $ f(n) = (-1)^n n = (e^(i pi))^n n = e^(i pi n) n $
  כלומר $f$ היא שלמה כהרכבה ומכפלה של פונקציות שלמות.
]

#subquestion()
$f(1/n^2) = 1/n$ לכל $n in NN$.

#proof[
  ראשית נשים לב שמתקיים
  $ f(0) = lim_(n arrow.r infinity) f(1/n^2) = 0 $
  אם נניח בשלילה שקיימת $f$ כזאת אז היא צריכה להיות גזירה בראשית, כלומר
  $
    f'(0) = lim_(n arrow.r infinity) (f(1/n^2)-f(0))/(1/n^2-0) = lim_(n arrow.r infinity) (1/n)/(1/n^2) = lim_(n arrow.r infinity) n = infinity
  $
  וזאת סתירה, אז אין אחת כזאת.
]

#pagebreak()

#subquestion()
$f(1/n)=n/(n+1)$ לכל $n in NN$.

#proof[
  מתקיים
  $ f(1/n) = n/(n+1) = 1/(1/n + 1) $
  ולכן אם נגדיר $ g(z) colon.eq 1/(z+1) $
  $f, g$ אנליטיות ב־$CC without {-1}$ ואם נבחר $z_n = 1/n$ לכל $n in NN$ ממשפט היחידות השני נקבל $f(z)=g(z)$.\
  כלומר
  $ f(z) = 1/(z+1) $
  אבל
  $ lim_(z arrow.r -1) 1/(z+1) = infinity $
  אז $g(z)$ לא הולומורפית ב־$CC$ ולכן בוודאי לא שלמה ולא ייתכן שיש $f$ שלמה כזאת
]


#question()
בכל סעיף נמצא את התחום בו ניתן לפתח את הפונקציה לטור לורן סביב הנקודה הנתונה ונמצא את הפיתוח.\
טור לורן $sum_(n=-infinity)^infinity c_n (z-a)^n$ מתכנס בטבעת מהצורה
$ A_(r_minus)^(r_plus) (a) colon.eq {z in CC bar r_minus<abs(z-a)<r_plus} $
עבור $0<=r_minus<r_plus<=infinity$.

#subquestion()
$f(z)=1/(z^2+1)^2$ סביב $z_0=i$.

#solution[
  ראשית $f$ לא מוגדרת ב־$plus.minus i$ שכן
  $ (z^2+1)^2 = ((z-i)(z+i))^2 = (z-i)^2 (z+i)^2 $
  וזו נקודה סינגולרית מסוג קוטב שכן $ lim_(z arrow.r i) abs(1/((z-i)^2(z+i)^2)) = abs(1/(0 dot.op (2i)^2))=infinity $
  ולכן יש לנו שני תחומים שבהם הטור מתכנס $A_0^2$ שבו הטור לורן מתלכד עם הטור טיילור ו־$A_2^infinity$.
  #figure(
    image("../../../src/images/complex analysis ex09 question 2a.jpg", width: 30%, fit: "contain"),
    caption: "המחשה לתחום התכנסות",
  )
  ראשית ניזכר בפיתוח טיילור הידוע
  $ (star) space 1/(1+x) = sum_(n=0)^infinity (-1)^n x^n $
  אז אם נכתוב
  $ f(z) = 1/(z^2+1)^2 = 1/((z+i)(z-i))^2 = (z-i)^(-2) dot.op 1/(z + i)^2 = (z-z_0)^(-2) dot.op 1/(z+i)^2 $
  זה מבנה של טור טיילור
  $ (x-x_0)^k sum_(n=0)^infinity (f^(n)(x_0))/n! (x-x_0)^(n-k) $
  אז נפתח את טור טיילור של $1/(z+i)^2$ ונרצה להשתמש ב־$(star)$ ובאריתמטיקה של טורי טיילור עם הרכבה.\
  נשים לב שניתן לכתוב
  $ 1/(z+i)^2 = 1/(2i+(z-i))^2 = 1/(2i dot.op 1/(2i)(2i+(z-i)))^2 = -1/4 dot.op 1/(1+1/(2i) (z-i))^2 $
  נתעלם כרגע מה־$-1/4$ ונחזיר אותו בסוף התהליך כסקלר. \
  #colbreak()
  כמו־כן
  $ 1/(1+1/(2i) (z-i))^2 = dif/(dif z) ((-2i)/(1+1/(2i)(z-i))) $
  ולכן עם $(star)$ ומהרכבה של פולינומי טיילור
  $
    1/(1+1/(2i) (z-i))^2 & = sum_(m=1)^infinity (-2i) dot.op dif/(dif z) (1/(1+1/(2i)(z-i))) dot.op (-1)^m \
                         & = (-2i) sum_(m=1)^infinity (-1)^m dot.op (2i)^(-m) dot.op dif/(dif z) (z-i)^m \
                         & = sum_(m=1)^infinity (-1)^(m+1) dot.op (2i)^(-(m+1)) dot.op m dot.op (z-i)^(m-1) \
                         & = sum_(m=0)^infinity (-2i)^(-m) dot.op (m+1) dot.op (z-i)^m
  $
  כלומר מכך ש־$z_0 = i$ ועם ההחזרה של $-1/4$
  $
    f(z) = (z-z_0)^(-2) dot.op 1/(z + i)^2 = (z-z_0)^(-2) dot.op sum_(m=0)^infinity (-1/4) dot.op (-2i)^(-m) dot.op (m+1) dot.op (z-z_0)^m \
    = -1/4 sum_(m=0)^infinity (-2i)^(-m) dot.op (m+1) dot.op (z-z_0)^(m-2)
    =_(n = m+2) -1/4 sum_(n=-2)^infinity (-2i)^(-n+2) dot.op (n+3) dot.op (z-z_0)^n
  $
  היה יכול להיות יותר קצר אם היינו משתמשים בפיתוח טיילור של סדרה בינומיאלית (היה מקצר מ־"כמו־כן").\
  עבור $A_2^infinity$ התהליך דומה אך שונה כי $(star)$ נכון רק עבור $x in (-1, 1)$.\
  אז
  $ 1/(z+i)^2 = 1/(z-i + 2i)^2 = 1/((z-i)^2(1+(2i)/(z-i))^2) $
  $ f(z) = 1/(z-i)^4 (1+(2i)/(z-i))^(-2) $
  ונשתמש בפיתוח טיילור של סדרה בינומיאלית שלילית כי בתחום זה הפרמטר קטן מ־$1$ בערך מוחלט.\
  אז
  $ binom(-2, k) = (-1)^k (k+1) $
  ולכן
  $ (1+x)^(-2) = sum_(n=0)^infinity (-1)^n (n+1) x^n $
  ובמקרה שלנו
  $ (1+(2i)/(z-i))^(-2) = sum_(n=0)^infinity (-1)^n (n+1) ((2i)/(z-i))^n $
  ובסך־הכל
  $
    f(z) = 1/(z-i)^4 dot.op sum_(n=0)^infinity (-1)^n (n+1) ((2i)/(z-i))^n = sum_(n=0)^infinity (-1)^n (n+1) (2i)^n (z-i)^(n-4)
  $
]

#subquestion()
$f(z)=(z^2-6z+10)/(z^2-7z+12)$ סביב $z_0=2$.

#solution[
  ניתן לכתוב
  $ z^2-7z+12 = (z-3)(z-4) $
  וכן
  $ f(z) = (z^2-6z+10)/(z^2-7z+12) = (z^2-7z+12+z-2)/(z^2-7z+12) = 1+(z-2)/((z-3)(z-4)) = 1+1/(z-3)-1/(z-4) $
  ולכן הנקודות הסינגולריות הן $z=3, z=4$ והמרחק מ־$z_0$ הוא
  $ abs(3-2)=1, space abs(4-2)=2 $
  אז יש לנו שלושה תחומים בהם אנחנו יכולים לפתח
  $ abs(z-2)<1, space 1<=abs(z-2)<=2, space abs(z-2)>2 $
  ונכתוב כביטוי של $z-2$ את $f$
  $ 1/(z-3) = 1/((z-2)-1), space 1/(z-4) = 1/((z-2)-2) $
  נבחן את התחום $abs(z-2)<1$: אם $w=z-2$ נקבל
  $ f(z) = 1+ 1/(w-1) - 1/(w-2) $
  וניתן לכתוב
  $ 1/(w-1) = -1/(1-w) $ ולהשתמש בנוסחה לטור גיאומטרי עבור $abs(x)<1$
  $ 1/(1-x) = sum_(n=0)^infinity x^n $
  ומהתחום $abs(w)=abs(z-2)<1$ ונקבל
  $ 1/(1-w) = sum_(n=0)^infinity w^n ==> 1/(w-1) = - sum_(n=0)^infinity w^n $
  ובאופן דומה
  $ 1/(w-2) = -1/2 dot.op 1/(1-w/2) $
  ומתקיים
  $ abs(w)<1 ==> abs(w/2) < 1/2 < 1 $
  ולכן
  $ 1/(1-w/2) = sum_(n=0)^infinity (w/2)^n ==> 1/(w-2) = -1/2 sum_(n=0)^infinity (w/2)^n $
  כלומר
  $
    f(z) = 1-sum_(n=0)^infinity w^n + 1/2 sum_(n=0)^infinity (w/2)^n = 1-sum_(n=0)^infinity (z-2)^n + 1/2 sum_(n=0)^infinity ((z-2)/2)^n
  $
  נעבור לתחום הבא בו $1<abs(z-2)<2$: כדי שנמשיך עם הטור הגיאומטרי צריך $abs(q)<1$, אז ממה שראינו מקודם (צריך לתקף רק את המקרה הראשון)
  $
    1/(1-w) = 1/(-w(1-1/w)) = -1/w sum_(n=0)^infinity (1/w)^n = - sum_(n=0)^infinity 1/(w^(n+1)) = - sum_(n=1)^infinity w^(-n)
  $
  ולכן במקרה זה
  $
    f(z) = 1+ sum_(n=1)^infinity w^(-n)+ 1/2 sum_(n=0)^infinity (w/2)^n = 1 + sum_(n=1)^infinity (z-2)^(-n) + 1/2 sum_(n=0)^infinity ((z-2)/2)^n
  $
  וזה בעצם פיתוח לורן ולא פיתוח טיילור.\
  אחרון חביב נשאר לחשב את הפיתוח באינסוף כלומר $abs(w)>2$, כלומר
  $ abs(w)>1 ==> abs(1/w) < 1, space abs(w)>2 ==> abs(2/w) < 1 $
  בשביל המחובר הראשון נשתמש במקרה הקודם וצריך לפקטר את $-w/2$ כי $abs(w/2)$>1:  ניקח את המינוס מהמקדם של כפול מינוס חצי ונקבל
  $ -1/(1-w/2) = -1/(w/2 (2/w-1))=(w/2(1-2/w)) $
  ולכן
  $
    2/w dot.op 1/(1-2/w) = 2/w sum_(n=0)^infinity (2/w)^n = sum_(n=0)^infinity 2^(n+1)/w^(n+1) = sum_(n=1)^infinity (2/w)^n
  $
  ובסך־הכל
  $
    f(z) = 1+sum_(n=1)^infinity w^(-n) + 1/2 sum_(n=1)^infinity (2/w)^n = 1 + sum_(n=1)^infinity (z-2)^(-n) + 1/2 sum_(n=1)^infinity (2/(z-2))^n
  $
]

#question()
תהיי $f$ שלמה ולא קבועה.

#subquestion()
נוכיח ש־$f(CC)$ צפופה ב־$CC$.

#proof[
  ראשית בגלל ש־$f$ שלמה ואיננה קבועה נובע ממשפט ליוביל שהיא לא חסומה (כי אם בשלילה $f$ הייתה חסומה היינו עומדים בכל תנאי משפט ליוביל והיינו מקבלים ש־$f$ קבועה, בסתירה לכך שהיא לא).\
  בשביל צפיפות עלינו להוכיח שלכל $w in CC$ ו־$epsilon > 0$ יש $z in CC$ כך ש־$abs(f(z)-w)<epsilon$.\
  נניח בשלילה ש־$f(CC)$ איננה צפופה ב־$CC$ ולכן קיימים $w_0 in CC$ ו־$epsilon > 0$ כך שלכל $z in CC$ מתקיים $abs(f(z)-w_0)>=epsilon$.\
  נגדיר
  $ h(z) colon.eq f(z)-w_0, space g(z) colon.eq 1/h(z) = 1/(f(z)-w_0) $
  ומכך ש־$h(z)$ אף־פעם לא מתאפסת מההנחה אז $g(z)$ מוגדרת היטב.\
  בנוסף, מכך ש־$f$ שלמה והמכנה אינו מתאפס נובע כי גם $g(z)$ שלמה כהרכבה של פונקציות שלמות ומתקיים
  $ abs(g(z)) = abs(1/(f(z)-w_0)) <= 1/epsilon $
  כלומר, $g(z)$ היא פונקציה קבועה ולכן אנחנו עומדים בתנאי משפט ליוביל ולכן קיים $c in CC$ כך שמתקיים $ forall z in CC, g(z) = c $
  ונובע מכך
  $ g(z) = c <==> 1/(f(z)-w_0) = c <==> f(z) = 1/c + w_0 $
  אבל זה בידיוק אומר ש־$f(z)$ היא פונקציה קבועה וזאת סתירה ולכן $f(CC)$ צפופה ב־$CC$.
]

#subquestion()
נוכיח כי אם $g in Hol(U_a^*)$ בעלת סינגולריות עיקרית ב־$a in CC$ אז ל־$f compose g$ יש סינגולריות עיקרית ב־$a$.

#proof[
  מהנתון של־$g$ יש סינגולריות עיקרית ב־$a in CC$, ממשפט קזוראטי-ויירשטראס נובע כי $g(U_a^*)$ היא צפופה ב־$CC$.\
  $f$ היא שלמה ולא קבועה וצפופה ב־$CC$ ולכן $f(g(U_a^*))$ צפופה ב־$CC$ (כי פונקציה רציפה על קבוצה צפופה משמרת צפיפות).\
  לא יכולה להיות נקודה סינגולריות סליקה: סינגולריות סליקה קיימת אם ורק אם
  $ exists M>0, space exists delta>0 space abs((f compose g)(z))<=M space forall 0<abs(z-a)<delta $
  כלומר
  $ (f compose g)(U_a^*) subset {w in CC bar abs(w)<=M} colon.eq overline(D(0, M)) $
  אבל $overline(D(0,M))$ הוא דיסק חסום וסגור ולכן קומפקטי ב־$CC$.\
  אבל $K subset CC$ הוא צפוף ב־$CC$ אם ורק אם לכל $z in CC$ היא נקודה גבולית ב־$K$, ואם $K$ הוא גם קומפקטי וגם צפוף אז הסגור שלו הוא עצמו מקומפקטיות אבל גם $CC$ מצפיפות, כלומר $K=CC$ שזו לא קבוצה קומפקטית ולכן סתירה, כלומר אף קבוצה קומפקטית איננה צפופה ב־$CC$ ו־$(f compose g)(U_a^*)$ היא צפופה כפי שראינו ולכן נקבל סתירה וזו אינה נקודה סינגורלית עיקרית.\
  לא יכולה להיות נקודה סינגולרית מסוג קוטב: אם היה קוטב, היה מתקיים
  $ exists R > 0, space exists delta>0 space abs((f compose g)(z))>R space forall 0<abs(z-a)<delta $
  כלומר
  $ (f compose g)(U_a^*) subset {w in CC bar abs(w)>R} $
  אבל ${w in CC bar abs(w)>R}$ אינה צפופה כי המשלים שלה הוא ${w in CC bar abs(w)<=R}$ שהיא קבוצה פתוחה לא ריקה וזו סתירה לצפיפות.\
  אז $a$ היא נקודת סינגולריות של $f compose g$ שאיננה סליקה או קוטב ולכן נקודת סינגולריוות עיקרית.
]

#question()
תהיינה $f,g$ שלמות ונניח שקיים קבוע $C>0$ כך שלכל $z$ מתקיים $abs(f(z))<=C abs(g(z))$.\
נוכיח כי קיים $lambda in CC$ עם $abs(lambda)<=C$ כך ש־$f(z)=lambda g(z)$.

#proof[
  נגדיר
  $ E = {z bar g(z) != 0} $
  ונגדיר $h:E arrow.r CC$ על־ידי
  $ h(z) colon.eq (f(z))/(g(z)) $
  מהנתון
  $ abs(h(z))=abs(f(z))/abs(g(z))<=C $
  כלומר $h$ חסומה בכל תחום הגדרתה.\
  תהיי $z_0 in CC without E$, כלומר $g(z_0)=0$, מהנתון
  $ (star) space abs(f(z))<=C abs(g(z)) $
  נובע כי הגבול
  $ lim_(z arrow.r z_0) (f(z))/(g(z)) $
  קיים וסופי: שכן מהיות $g$ שלמה, נקודות ההתאפסות שלה הן מבודדות, כלומר אם $g(z_0)=0$ אז קיימת סביבה של $z_0$ שלכל $z$ בה, $g(z)!=0$.\
  מטענה שראינו, קיימים $m,n>=0$ ופונקציות הולומורפיות $F,G$ כך שמתקיים
  $
    g(z)=(z-z_0)^m G(z), space G(z_0)!=0 \
    f(z)=(z-z_0)^n F(z), space F(z_0) != 0 space "or" space f eq.triple 0
  $
  כאשר $m,n$ הם הסדרים של $0$ של $f,g$ ב־$z_0$, בהתאמה.\
  אז מההנחה $(star )$ נקבל
  $ abs((z-z_0)^n F(z))<=C abs((z-z_0)^m G(z)) <==> abs(z-z_0)^(n-m) <= C abs(G(z))/abs(F(z)) $
  אם $F$ זהותית אפס לא נחלק וסיימנו. אחרת, $F(z_0)!=0$ ו־$G(z_0)!=0$ ולכן $abs(G(z))/abs(F(z))$ חסומה בסביבה של $z_0$.\
  אם $n>m$ אז $abs(z-z_0)^(n-m) stretch(arrow.r)_(z arrow.r z_0) infinity$ וזו סתירה, אז $n>=m$.\
  אז
  $ (f(z))/(g(z)) = (z-z_0)^(n-m) (F(z))/(G(z)) $
  כאשר המנה היא הולומורפית כמנת פונקציות הולומורפיות ו־$n-m>=0$ ולכן
  $ lim_(z arrow.r z_0) (f(z))/(g(z)) = mycases(0, n>m, (F(z_0))/(G(z_0)), n=m) $
  בכל מקרה הגבול קיים וסופי ובפרט נובע כי $z_0$ היא נקודה סינגולרית סליקה ולכן יש המשכה הולומורפית ל־$h$ ואז $h$ היא פונקציה שלמה על $CC$.\
  פונקציה שלמה וחסומה מקיימת את משפט ליוביל ולכן היא קבועה, כלומר קיימת $lambda in CC$ כך שמתקיים
  $ forall z in CC, space h(z)=lambda ==> f(z) = lambda g(z) $
  בפרט מ־$(star)$ נובע $abs(h)<=C ==> abs(lambda)<=C$.
]

#question()
תהיי $f: CC arrow.r CC$ שלמה וחד־חד ערכית ונגדיר $g: CC without {0} arrow.r CC$ על־ידי $g(z)=f(1/z)$.

#subquestion()
נוכיח שהסינגולריות של $g$ בראשית אינה סליקה.

#proof[
  נניח בשלילה כי $z=0$ היא נקודה סינגולרית סליקה ולכן קיימת לה המשכה הולומורפית, כלומר קיימת
  $ forall z!=0, space accent(g, tilde) (z) = f(1/z) $
  ובפרט הגבול הבא קיים וסופי
  $ lim_(z arrow.r 0) f(1/z) = accent(g, tilde) (0) $
  כאשר $z arrow.r 0$ מתקיים $1/z arrow.r infinity$ כלומר הגבול שקיים וסופי היינו
  $ lim_(w arrow.r infinity) f(w) $
  נניח שהוא שווה ל־$L$ ונגדיר
  $ h(w) colon.eq f(w)-L $
  אז $h$ היא שלמה ומתקיים
  $ (star) space h(w) stretch(arrow.r)_(w arrow.r infinity) 0 $
  אז $h$ חסומה על $CC$ ולכן ממשפט ליוביל ומ־$(star)$ נובע כי $h eq.triple 0$, כלומר $f(w) eq.triple L$ וזו סתירה לחד־חד ערכיות ולכן $z=0$ איננה סינגולריות סליקה.
]

#subquestion()
נוכיח שהסינגולריות של $g$ בראשית אינה עיקרית.

#proof[
  נניח בשלילה שהסינגולריות בראשית היא עיקרית ולכן ממשפט קזוראטי-ויירשטראס $g(U_0^*) = f(1/(U_0^*))$
  היא צפופה ב־$CC$.\
  נגדיר
  $ h(z) colon.eq f(z)-f(0) $
  אז $h$ שלמה כי $f$ שלמה ו־$h(0)=0$ ומהיות $f$ חד־חד ערכית נובע כי $0$ הוא המקור היחידי לאפס של $h$.\
  יתר על־כן, נובע כי הסדר של $0$ הוא $m>=1$ וסופי, כלומר קיימת פונקציה שלמה $H$ המקיימת
  $ H(0)!=0, space h(z)=z^m H(z) $
  מהצפיפות קיימת סדרה $(w_n)_(n=1)^infinity$ כך שמתקיים $f(w_n) stretch(arrow.r)_(n arrow.r infinity) f(0)$ ובפרט $h(w_n) arrow.r 0$.\
  נגדיר
  $ F(z) colon.eq h(z)/z^m = H(z) $
  אז $F$ שלמה ונקבל מכך ש־$abs(w_n)^m arrow.r infinity$
  $ F(w_n) = (h(w_n))/w_n^n stretch(arrow.r)_(n arrow.r infinity) 0 $
  אז $F$ חסומה על סדרה לא חסומה כלומר חסומה על כל $CC$ ולכן ממשפט ליוביל נקבל $F eq.triple C$ עבור $C$ קבוע כלשהו אבל מהיות $F(w_n) arrow.r 0$ נובע כי $C = 0$ ולכן $H eq.triple 0$ כלומר $h eq.triple 0$ וזה אומר $f(z) eq.triple f(0)$ בסתירה לחד־חד ערכיות, אז הראשית היא לא נקודה סינגולרית עיקרית.
]
#pagebreak()

#subquestion()
נסיק שקיימים קבועים $a!=0$ ו־$b$ כך ש־$f(z)=a z + b$

#proof[
  מהסעיפים הקודמים נובע כי בראשית $z=0$ היא נקודה סינגולרית מסוג קוטב של $g$.\
  אם יש ל־$g$ קוטב מסדר $m$ בראשית נובע
  $ g(z)=c_(-m)/z^m + c_(-(m-1))/z^(m-1) + dots.h.c + c_0 + c_1 z + dots.h.c $
  נציב $z=1/w$ ונקבל
  $ f(w)=c_(-m) w^m + c_(-(m-1)) w^(m-1) + dots.h.c + c_0 + c_1 w^(-1) + dots.h.c $
  אבל $f$ שלמה ולכן כל החזקות השליליות של $w$ נעלמות (כי מהשלמות נובע הולומורפיות על כל $CC$ והולומורפיות גוררת שניתן להציג בצורה לוקאלית לפי טור טיילור)
  אז
  $ f(w) = c_(-m) w^m + c_(-(m-1)) w^(m-1) + dots.h.c + c_0 $
  כלומר $f$ היא פולינום מדרגה $m$.\
  נניח ש־$m>=2$, אז
  $ f'(z)=m c_(-m) z^(m-1) + dots.h.c $
  הוא פולינום לא קבוע ולכן יש לו לפחות אפס  אחד ב־$CC$ מטענה שראינו על קיום שורש.\
  אז עבור $z_0$ כלשהי מתקיים $f'(z_0)=0$ אבל זה אומר ש־$f$ לא חד־חד ערכית בסביבה של $z_0$ $(star)$ אבל $f$ חד־חד ערכית בכל תחומה אז $m=1$ כי $f$ לא קבועה אז החזקה לא יכולה להיות $0$ ולהיות פולינום קבוע.\
  אז $f(z) = a z + b$ עבור $a!=0$ (אחרת זה שקול ל־$m=0$ שראינו שלא ייתכן).\
  הסבר קצר על $(star)$ כי אני לא זוכרת שראינו: בגלל ש־$f'(z_0)=0$ אז $a_1=0$ ובגלל ש־$f$ לא קבועה אז קיים $NN in.rev k>=2$ כך ש־$a_k != 0$ ולכן
  $ f(z)=f(z_0)+a_k (z-z_0)^k + a_(k+1) (z-z_0)^(k+1) + dots.h.c ==> f(z) = f(z_0) + (z-z_0)^k h(z) $
  כאשר $h(z)$ הולומורפית ו־$h(z_0)=a_k != 0$.\
  אז אם ננסה לפתור את המשוואה $f(z)=f(z_0)+epsilon$ עבור $epsilon!=0$ עם הפקטור שראינו לעיל על־ידי
  $ (z-z_0)^k = epsilon/(h(z)) $
  וכאשר $epsilon arrow.r 0$ אגף ימין שואף לאפס ולמשוואה $w^k=c$ עבור $c!=0$ יש ב־$CC$ בידיוק $k>=2$ פתרונות שונים, וזו סתירה לחד־חד ערכיות של $f$.

]
